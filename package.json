{
  "name": "microfrontend-boilerplate",
  "private": true,
  "workspaces": [
    "host",
    "products",
    "cart"
  ],
  "scripts": {
    "start": "npm-run-all --parallel start:host start:products start:cart",
    "start:host": "npm --prefix host start",
    "start:products": "npm --prefix products start",
    "start:cart": "npm --prefix cart start",
    "build": "npm install && npm run build:all && npm run prepare-deploy",
    "build:all": "npm-run-all build:host build:products build:cart",
    "build:host": "npm --prefix host run build",
    "build:products": "npm --prefix products run build",
    "build:cart": "npm --prefix cart run build",
    "prepare-deploy": "node deploy.js"
  },
  "devDependencies": {
    "fs-extra": "^11.3.2",
    "npm-run-all": "^4.1.5"
  }
}
